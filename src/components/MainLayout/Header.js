import React from 'react';
import { NavBar } from 'antd-mobile';

function Header({ location }) {
  return (
    <div>
    <NavBar
      mode="light"
    >民信贷</NavBar>
  </div>
  );
}

export default Header;
